import { WhatsappDatePipe } from './whatsapp-date-pipe';

describe('WhatsappDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WhatsappDatePipe();
    expect(pipe).toBeTruthy();
  });
});
